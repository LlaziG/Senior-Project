const candles = require('./candles');
const accounts = require('./accounts');
const transactions = require('./transactions');
const wallets = require('./wallets');

module.exports = { candles, accounts, transactions, wallets }